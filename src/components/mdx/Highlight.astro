---
export interface Props {
  color?: 'yellow' | 'green';
}

const { color = 'yellow' } = Astro.props;
const className = color === 'green' ? 'highlighter-green font-bold text-indigo-700' : 'highlighter-yellow font-bold text-indigo-700';
---

<span class={className}><slot /></span>
